server:
  port: 9001

spring:
  application:
    name: nacos-payment-provider
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848 # 配置nacos服务中心注册地址

# 暴露监控端口
management:
  endpoints:
    web:
      exposure:
        include: '*'
